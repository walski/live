Live.IndexController = Ember.Controller.extend({
  // submitComment: function() {
  //   article = this.get('model');
  //   data = this.getProperties('body', 'parent');
  //   comment = article.get('comments').createRecord(data);
  //   comment.transaction.commit();

  //   this.resetForm();
  // },

  // resetForm: function() {
  //   this.setProperties({body: '', parent: undefined});
  // },

  // setParent: function(comment) {
  //   this.set('parent', comment);
  // }
});
