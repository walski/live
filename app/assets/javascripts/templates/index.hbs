{{#if isDevelopment}}
<div id="now" class="alert alert-info">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  <em>It's now {{dateTime App.currentTime}}</em>
</div>
{{/if}}

{{render 'sessions'}}
{{render 'trips'}}
{{render 'directions'}}
{{render 'contacts'}}
