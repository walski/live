<div id="now" class="alert alert-info">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  <em>It's now {{dateTime App.currentTime}}</em>
</div>

{{render 'sessions' controllers.sessions}}
{{render 'trips' controllers.trips}}
{{render 'directions' controllers.directions}}
{{render 'contacts' controllers.contacts}}
